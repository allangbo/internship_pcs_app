import 'package:flutter/material.dart';

class ListVacanciesPage extends StatelessWidget {
  const ListVacanciesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
